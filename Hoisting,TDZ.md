# Hosting
- Hoisting is JavaScript's behavior of moving declarations (not initializations) to the top of their scope (global or function) during the compile phase.
# TDZ
- The TDZ is the time between entering a block scope and when a let or const variable is declared and initialized. Accessing the variable in this period causes a ReferenceError.
